# 架构说明

普通用户只需要使用 `输入/媒体素材/直播录像/`、`输出/最终成片/` 和根目录 `README.md`。

`skills/extract-livestream-clips/` 定义审核门禁；`scripts/` 负责候选评分、连续区间审核、派生母版和最终渲染；`引擎/remotion/` 只注册直播切点审核、最终视觉审核和 9:16 成片模板。

原片不可修改。每个已批准候选生成一条独立派生母版和重映射逐字稿，时长锁定后才进入竖屏模板。候选审核数据、视觉审核数据和最终标题确认数据严格分离。
